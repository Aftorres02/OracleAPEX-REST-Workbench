-- Listing for triggers
-- Defers to the explicit installer under triggers/ (order does not matter
-- between compound triggers -- each only depends on its own table already
-- existing -- but this keeps one authoritative file list instead of two).
@../triggers/_install_arw_triggers.sql
